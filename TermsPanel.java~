import java.awt.*;
import javax.swing.*;

public class TermsPanel extends JPanel
{
  //private SpecialMatrix specialMatrix;
  private CalculatorPanel cp;
   //-----------------------------------------------------------------
   //  Sets up this panel with two labels.
   //-----------------------------------------------------------------
   public TermsPanel(CalculatorPanel cp)
   {
     this.cp = cp;
     //specialMatrix = new SpecialMatrix(cp);
     
      //setBackground (Color.blue);

      JLabel l1 = new JLabel ("<html><h2 style ='font-family:Marker felt;color:#33CC33;font-size:36px;align:center'>Informaiont about your matrix</h2>");
      //JLabel l2 = new JLabel (specialMatrix.linkedlist());
      add (l1);
      //add (l2);
   }
}